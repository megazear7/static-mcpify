# static-mcpify starters

- `netlify-static-starter` — filesystem content starter with preloaded sample content and `/mcp`
- `netlify-contentful-starter` — bundled sample content plus optional Contentful rebuilds on deploy
